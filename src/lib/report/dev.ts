// Cookie служебной «имитации сбоя ИИ» (/dev/ai-fail): пока она стоит, генерация отчёта в этом
// браузере получает от «ИИ» не-JSON и уходит в «не удалось». Работает только вне боевого сайта.
export const AI_FAIL_COOKIE = "yw_dev_ai_fail";
